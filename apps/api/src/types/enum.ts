export enum NormalPageTemplate {
  DEFAULT = '',
  LIST = 'list',
  GRID_CARD = 'grid-card', // 前端默认展示
}

export enum WorkPageTemplate {
  DEFAULT = '',
  VIDEO = 'video',
  MANGA = 'manga',
  ALBUM = 'album',
  MUSIC = 'music',
  EBOOK = 'ebook',
  MIXTURE = 'mixture', // 前端默认展示
}
