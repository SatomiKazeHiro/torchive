/**
 * @deprecated 直接从 `@/api` 导入。`@/api/web` 保留为向后兼容 shim。
 *
 * 实际实现已按域拆分到 `./shared` `./works` `./domains` `./categories`
 * `./details` `./users` `./user-favorites` `./user-histories` `./user-watch-laters`
 */
export * from "./index";